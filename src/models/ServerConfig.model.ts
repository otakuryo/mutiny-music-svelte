export interface ServerConfig {
    username: string
    password: string
    serverUrl: string
    serverName: string
    serverType: string
    serverVersion: string

    defaultPlaylistId?: string
    defaultPlaylistName?: string
}